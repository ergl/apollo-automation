{run_template, "run.config.template"}.

{experiments,
    [
        #{
            config_file => "cluster.config",
            clients => {lists, seq, [1,1,1]},
            results_folder => "scalability+k_2",
            run_with => #{
                operations => [{read_distinct,1}],
                readonly_ops => 2,
                retry_aborts => false
            }
        }
    ]
}.
